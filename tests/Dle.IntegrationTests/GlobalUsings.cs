// Usings shared by every file in the integration suite. The project has ImplicitUsings enabled, so
// only what the framework does not already supply is listed here.
global using Dle.IntegrationTests.Infrastructure;

global using Xunit;
