<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted to the emulator's host alias only, so a developer can point the sample at
  a dle-control running on the workstation. Every other destination stays https-only, which is
  also what the SDK enforces for its own endpoint (DleConfig).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
