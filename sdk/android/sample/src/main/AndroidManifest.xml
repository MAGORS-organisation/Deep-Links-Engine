<?xml version="1.0" encoding="utf-8"?>
<!--
  Deep Link Engine — sample application.

  The SDK's own manifest contributes INTERNET; nothing else is needed.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SampleApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
          android:exported is stated explicitly (required from API 31 for any component with an
          intent filter). singleTop keeps one instance, so a link tapped while the app is open
          arrives through onNewIntent instead of creating a second screen.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Android App Links (spec §A.2.2, FR-144). autoVerify makes the system fetch
              https://link.example.sk/.well-known/assetlinks.json at install time and, when the
              SHA-256 of the signing certificate matches, open https://link.example.sk/... in this
              app directly, with no chooser. Only https and only the exact host: a custom scheme
              would let any application claim the same links, and carries no verification.

              Replace link.example.sk with the domain dle-edge serves. The fingerprint in
              assetlinks.json must be the one Google Play signs the release with (Play Console >
              App integrity), not the upload keystore's; see README.md.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="link.example.sk" />
            </intent-filter>
        </activity>
    </application>

</manifest>
